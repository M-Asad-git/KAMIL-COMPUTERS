# API Configuration - CRITICAL for frontend to work!
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Next.js Configuration
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# IMPORTANT: Frontend should NEVER connect directly to database
# All database operations go through the backend API server
# This file tells your admin app where to find the API server
